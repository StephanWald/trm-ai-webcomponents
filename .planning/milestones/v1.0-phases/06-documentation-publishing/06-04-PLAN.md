---
phase: 06-documentation-publishing
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - docs/docs/components/sp-org-chart.mdx
  - docs/docs/components/sp-walkthrough.mdx
  - docs/docs/components/sp-markdown-editor.mdx
  - docs/docs/changelog.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
  - DOCS-06

must_haves:
  truths:
    - "Each component page shows auto-generated API reference tables (props, events, methods, CSS custom properties, CSS parts) rendered from docs.json"
    - "Each component page has 3-5 interactive live examples showing basic setup, common configurations, event handling, and theming"
    - "Developer can edit example source code and see the preview update in real time"
    - "Changelog page documents release history and links to release notes"
    - "Complete Docusaurus site builds successfully with all pages"
  artifacts:
    - path: "docs/docs/components/sp-org-chart.mdx"
      provides: "OrgChart component documentation with examples and API reference"
      contains: "ApiReference"
    - path: "docs/docs/components/sp-walkthrough.mdx"
      provides: "Walkthrough component documentation with examples and API reference"
      contains: "ApiReference"
    - path: "docs/docs/components/sp-markdown-editor.mdx"
      provides: "Markdown Editor component documentation with examples and API reference"
      contains: "ApiReference"
    - path: "docs/docs/changelog.md"
      provides: "Changelog page for release tracking"
      contains: "Changelog"
  key_links:
    - from: "docs/docs/components/*.mdx"
      to: "docs.json"
      via: "JSON import for API reference data"
      pattern: "import.*docs\\.json"
    - from: "docs/docs/components/*.mdx"
      to: "docs/src/components/LiveExample.tsx"
      via: "MDX component import"
      pattern: "import LiveExample"
    - from: "docs/docs/components/*.mdx"
      to: "docs/src/components/ApiReference.tsx"
      via: "MDX component import"
      pattern: "import ApiReference"
---

<objective>
Create the three component documentation pages (MDX) with auto-generated API reference and interactive live examples, plus a changelog page for version tracking.

Purpose: DOCS-02 requires auto-generated API docs per component. DOCS-03 requires interactive live examples per component. DOCS-06 requires documentation versioning — implemented as a changelog page with a note about adding full Docusaurus versioning when the project reaches v1.0.0.

Output: Three component MDX pages with complete API reference + live examples; changelog page; fully buildable documentation site.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-publishing/06-RESEARCH.md
@.planning/phases/06-documentation-publishing/06-01-SUMMARY.md
@.planning/phases/06-documentation-publishing/06-03-SUMMARY.md
@docs.json
@docs/docusaurus.config.ts
@docs/sidebars.ts
@docs/src/components/LiveExample.tsx
@docs/src/components/ApiReference.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sp-org-chart and sp-walkthrough component pages</name>
  <files>
    docs/docs/components/sp-org-chart.mdx
    docs/docs/components/sp-walkthrough.mdx
  </files>
  <action>
    Create MDX pages for the two simpler components. Each page follows this pattern:

    1. **Frontmatter**: title, description, sidebar_position
    2. **Imports**: docs.json (relative path to where Stencil writes it), LiveExample, ApiReference
    3. **Component filter**: `const componentDocs = docsData.components.find(c => c.tag === 'sp-xxx');`
    4. **Introduction**: 1-2 sentence description of the component
    5. **Live Examples**: 3-5 examples using `<LiveExample>` component
    6. **API Reference**: `<ApiReference component={componentDocs} />`

    **sp-org-chart.mdx** (sidebar_position: 1):
    - Example 1: Basic org chart — flat user array with reportsTo relationships (3-5 users), default styling
    - Example 2: Editable mode — same data with `editable="true"`, note about drag-and-drop
    - Example 3: Filtered view — org chart with `filter="Manager"` prop showing filter behavior
    - Example 4: Event handling — org chart with inline `<script>` listening to `user-click` and `hierarchy-change` events, logging to console
    - Example 5: Dark mode — org chart wrapped in a div with `class="theme-dark"` or showing custom CSS properties

    **sp-walkthrough.mdx** (sidebar_position: 2):
    - Example 1: Basic walkthrough — simple walkthrough with 2-3 scenes, no video (manual navigation)
    - Example 2: With video — walkthrough with an MP4/video source and timed scenes
    - Example 3: Author mode — walkthrough with `author-mode="true"` showing editing capabilities
    - Example 4: Event handling — walkthrough with event listeners for scene-changed, walkthrough-shown/hidden
    - Note: Walkthrough examples may render partially in docs preview since they depend on external DOM elements for highlighting — add a note explaining this

    Each example should include the CDN script tag in the HTML code passed to LiveExample. Keep example HTML self-contained and working.

    For the import path to docs.json: use a relative path from the MDX file location. Since docs.json is generated at repo root by Stencil, and the MDX files are in docs/docs/components/, the import path should be `../../../docs.json` (or adjust based on where Plan 01 actually places docs.json — check the 06-01-SUMMARY for the actual path).
  </action>
  <verify>
    - `ls docs/docs/components/sp-org-chart.mdx docs/docs/components/sp-walkthrough.mdx` both exist
    - Files contain `<LiveExample` and `<ApiReference` components
    - `grep -c "LiveExample" docs/docs/components/sp-org-chart.mdx` returns 3+ (at least 3 examples)
    - `grep -c "LiveExample" docs/docs/components/sp-walkthrough.mdx` returns 3+ (at least 3 examples)
  </verify>
  <done>
    sp-org-chart.mdx has 4-5 interactive live examples + auto-generated API reference; sp-walkthrough.mdx has 3-4 interactive live examples + auto-generated API reference; both import and use docs.json data correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create sp-markdown-editor page and changelog</name>
  <files>
    docs/docs/components/sp-markdown-editor.mdx
    docs/docs/changelog.md
  </files>
  <action>
    1. **sp-markdown-editor.mdx** (sidebar_position: 3):
       Follow the same pattern as Task 1.

       - Example 1: Basic editor — minimal markdown editor with default configuration
       - Example 2: With initial content — editor with `content` prop set to sample markdown text
       - Example 3: Toolbar customization — editor with initial content showing formatting toolbar
       - Example 4: Event handling — editor with event listeners for `content-change`, `save`, `mode-change`
       - Example 5: Dark mode — editor with theme-dark class or custom CSS properties

       Important note for examples: sp-markdown-editor requires peer dependencies (marked, DOMPurify, Prism.js) to render WYSIWYG/preview mode. In CDN examples, these must be loaded via separate script tags OR the examples should focus on source mode which works without peer deps. Add a note in the page about peer dependency requirements for full functionality.

       Peer dependency CDN scripts for full examples:
       ```html
       <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
       <script src="https://cdn.jsdelivr.net/npm/dompurify/dist/purify.min.js"></script>
       <script src="https://cdn.jsdelivr.net/npm/prismjs/prism.min.js"></script>
       ```

    2. **docs/docs/changelog.md** — Create a changelog page:
       - Frontmatter: `title: Changelog`, `sidebar_position: 10`
       - Introduction: "This page tracks changes across releases of @skillspilot/webcomponents."
       - Current version section: `## 0.0.1 (Initial Development)` with brief summary of what's been built (3 components, DWC theming, etc.)
       - Note: "This project uses [Changesets](https://github.com/changesets/changesets) for version management. Future releases will have automatically generated changelogs."
       - Note about versioning: "Full Docusaurus documentation versioning (version picker, historical API references) will be added when the project reaches v1.0.0 with breaking API changes. Until then, this changelog and the latest documentation serve as the version reference."

    This satisfies DOCS-06 (documentation versioned/synced with releases) via the changelog approach — appropriate for an early-stage project with 3 components at v0.0.1.
  </action>
  <verify>
    - `ls docs/docs/components/sp-markdown-editor.mdx docs/docs/changelog.md` both exist
    - `grep -c "LiveExample" docs/docs/components/sp-markdown-editor.mdx` returns 3+ (at least 3 examples)
    - `grep -c "Changesets" docs/docs/changelog.md` returns at least 1
    - `cd docs && npm run build` — full Docusaurus build should now complete successfully with all pages
  </verify>
  <done>
    sp-markdown-editor.mdx has 4-5 interactive live examples + API reference + peer dependency notes; changelog.md documents current version and versioning strategy; complete Docusaurus site builds successfully.
  </done>
</task>

</tasks>

<verification>
1. All three component MDX pages exist in docs/docs/components/
2. Each component page imports docs.json and renders ApiReference
3. Each component page has 3-5 LiveExample interactive demos
4. changelog.md documents versioning strategy
5. `cd docs && npm run build` completes successfully
6. Built site in docs/build/ has HTML files for all pages
</verification>

<success_criteria>
- Every component has auto-generated API reference from docs.json (DOCS-02)
- Every component has 3-5 interactive live examples with editable code (DOCS-03)
- Changelog page tracks releases and documents versioning approach (DOCS-06)
- Complete documentation site builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation-publishing/06-04-SUMMARY.md`
</output>
